# Environment Variables for Backend

# MongoDB Configuration
MONGODB_URI=mongodb+srv://harmit485:Hpk1234BMP@cluster0.quhjx.mongodb.net/carbon_credit_db?retryWrites=true&w=majority

# JWT Configuration
JWT_SECRET=your-super-secret-jwt-key-mlfjlust-be-at-least-256-bits-long
JWT_EXPIRATION_MS=86400000

# CORS Configuration (comma-separated list of allowed origins)
CORS_ALLOWED_ORIGINS=https://your-app.vercel.app,http://localhost:5173

# Server Configuration (automatically set by Render, but can override for local dev)
PORT=8080

